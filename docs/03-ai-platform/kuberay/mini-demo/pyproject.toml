[project]
name = "kuberay-mini"
version = "0.1.0"
description = "CPU-runnable KubeRay control-plane mini demo"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
