[project]
name = "kubeflow-mini"
version = "0.1.0"
description = "Pure-Python mini-demo simulating Kubeflow control-plane resources"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
